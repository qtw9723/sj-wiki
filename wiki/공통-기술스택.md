---
title: 공통 기술 스택 (개인 프로젝트)
category: 도구/스킬
tags: [스택, react, vite, tailwind, supabase, 도구]
source: raw/README.md, raw/projects.json
created: 2026-06-09
updated: 2026-09-05
---

> [!tip] 핵심 takeaway
> **한 번 깊게 익히면 7개 프로젝트에 전부 통하는 너의 "주력 무기 세트"** — React 19 + Vite 8 + Tailwind 4 + Supabase.
> [[내-프로필]]의 "개발자 성장" 목표에서 **여기에 학습을 집중하는 게 ROI가 가장 높다.** 스택이 통일돼 있어 한 프로젝트의 개선이 곧 전 프로젝트의 개선이 된다.
> 약점은 **단조로움** — 모든 앱이 같은 스택이라, 도구 업그레이드/패턴 변경 시 일괄 작업이 잦다. 이건 [[에이전트-자동화-도구]]로 자동화하기 좋은 조건.

## 표준 스택
**프론트엔드**
- React 19 (전 프로젝트)
- Vite 8 (빌드)
- Tailwind CSS 4 (스타일)
- lucide-react (아이콘), react-router-dom (라우팅)

**백엔드/데이터**
- Supabase (PostgreSQL + Auth + Edge Functions) → [[parking]]에서 공유 관리. Edge Functions(Deno)는 [[CogInsight-Generator]](생성·학습·인증 20개 함수)·[[mailer]]·[[notepad]] 등이 직접 사용.
- Express.js ([[mailer]], [[schedule-reporter-kakao]] — Node 서버 풀스택)
- Nodemailer (메일 발송)
- 🧠 [[CogInsight-Generator]]는 더 이상 프론트 전용이 아니라 **Supabase Edge Functions 백엔드를 갖춘 풀스택**(LLM 호출=OpenAI gpt-4o를 엣지함수 안에서).

**자주 쓰는 라이브러리**
- @dnd-kit (드래그앤드롭) — notepad·mailer·schedule-reporter 공통
- zod (스키마 검증) — 특히 [[CogInsight-Generator]]의 Dialog JSON 검증
- react-hook-form, Radix UI / shadcn (CogInsight)
- react-markdown + remark-gfm ([[notepad]])

## 프로젝트별 스택 매핑
| 라이브러리 | 쓰는 곳 |
|---|---|
| Express + Nodemailer | [[mailer]], [[schedule-reporter-kakao]] |
| react-markdown | [[notepad]] |
| zod + Radix/shadcn | [[CogInsight-Generator]] |
| Supabase Edge Functions(Deno) | [[CogInsight-Generator]](생성·인증), [[notepad]]·[[mailer]] / 호스팅=[[parking]] |
| @dnd-kit | notepad, mailer, schedule-reporter |
| Supabase | 전부 (via [[parking]]) |

## 학습/개선 포인트
- **zod**는 챗봇 시나리오 검증([[CogInsight-Generator]])과 LLM 출력 검증에 핵심 → 우선 깊게. 실무 API/DB 스키마를 검증 대상으로 삼으면 효과적.
- 스택 통일의 단점(반복 업그레이드)은 [[Claude-Code-업데이트-동향]]의 dynamic workflows로 "전 프로젝트 일괄 마이그레이션" 자동화 가능.

> 개인 프로젝트 스택 외의 도구·MCP 커넥터(Cowork·카카오 Play 등)는 [[내-MCP-커넥터-환경]] 참고.

## 🆕 🚨 스택에 없던 항목 — **에이전트 운영 비용** (2026-09-05 추가 · 11차 감사)

> 🚨 이 절은 [[AI-주간-소식-2026-W36]] 10차 자동 패스가 *"✅ [[공통-기술스택]] 운영 비용 항목 업데이트"* 라고 **선언만 하고 실행하지 않은** 항목이다(이 문서는 그 시점까지 `updated: 2026-06-25`로 **두 달 넘게 정지**해 있었다). 11차에서 실제 집행한다.

📄 **W36 한 주에 비용 계보가 통째로 나왔다** (전부 [[AI-주간-소식-2026-W36]] 근거):

| 층 | 무엇이 병목인가 | 📄 근거 |
|---|---|---|
| 컨텍스트·캐시 | **비용 ≈ 호출수 × 호출당 토큰 × (1 − 캐시적중률)** — 컨텍스트를 잘라내면 프리픽스가 깨져 **토큰을 줄였는데 비용이 오른다** | (W36-EN3) TokenPilot |
| 호출 횟수 | 오류 시 **전체 재전송이 아니라 부분 교정** 하면 호출수 항이 직접 준다 (SQL 오류 87% 해결) | (KR-W36-68) KAIST SafeQL |
| 실효 성능 | GPU 스펙이 높아도 멀티에이전트 실처리량은 낮다(동기화 대기·컨텍스트 전달 오버헤드·유휴) | (KR-W36-49) |
| CPU | 학습 → **자율 에이전트·실시간 추론** 전환으로 호스트 CPU 수요 폭증, **MSRP 대비 1.8~2.2배 프리미엄** | (KR-W36-64)·(KR-W36-87) |
| DB 인프라 | *"1세대 AI 에이전트 플랫폼들이 **뇌(LLM)만 키우고 몸(DB 인프라)을 방치**했다"* | (KR-W36-80) EDB 서밋 2026 |
| 클라우드 총액 | **1인당 클라우드 비용 연 1억 5000만원** → AMD가 **로컬 AI**를 대안으로 제시 | (KR-W36-88) |

- ✅ 🧠 **이 표가 개인 스택에 주는 결론 한 줄**: **모델을 바꾸는 것보다 「호출을 줄이고 캐시를 살리는 설계」가 비용에 더 직접 작용한다** — 📄 W36 한 주에 모델이 4개 출시된 상황에서 모델 핀 추격은 유지비만 늘 수 있다([[내-MCP-커넥터-환경]] §5-2와 같은 결론).
- ✅ **적용 대상**: [[CogInsight-Generator]] `flow/usage.ts` 단가표가 **gpt-4o 기준**이라 비용 비교 기준선부터 갱신 필요(기존 TODO) → 🧠 갱신할 때 **단가만이 아니라 「호출수 · 캐시적중률」 두 칸을 같이** 재는 형태로 바꾸는 편이 위 공식과 맞는다.
- ⚠ 📄 위 수치는 전부 **기사 인용**이며 개인 프로젝트 규모에 그대로 적용되는 값이 아니다(단정 금지).

## 관련 문서
- 🆕 ⭐ [[AI-주간-소식-2026-W33]] **(EN47, 2026-08-18 보강) — 모델 선택에 「어느 실리콘에서 도는가」 축이 붙었다**: 📄 OpenAI **Ultrafast** 서비스 티어(GPT-5.6 Sol, **초당 최대 750 출력 토큰 · 최대 14배**)의 **백엔드가 Cerebras**다. 🧠 *"OpenAI가 빨라졌다"* 가 아니라 **"자기 스택 밖의 추론 하드웨어를 서비스 티어로 편입했다"** — 📄 한국어 보도((W34-KR2)(KR3))엔 없던 조각이다. 🧠 (W34-KR17) HBM 쇼티지·(W34-KR24) 우란차부와 **같은 축**(추론 공급을 어디서 사 오는가)이고, 기존 *"쓸 수 있는가"* 축(수출통제·파트너 게이팅·클라우드 채널)에 **네 번째 칸**으로 들어간다.
- [[프로젝트-포트폴리오]] · [[parking]] · [[CogInsight-Generator]] · [[에이전트-자동화-도구]] · [[내-MCP-커넥터-환경]]
