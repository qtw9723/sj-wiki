---
title: 올림푸스 (Olympus) — 로컬 다세션 자율 개발 파이프라인
aliases: [Olympus, 올림푸스, olympus, dev-pipeline]
category: 프로젝트
tags: [프로젝트, 올림푸스, 에이전트, 멀티에이전트, 오케스트레이션, claude-code, 자동화, 개발도구, 포트폴리오, bash, 생성자-비평가, 자가개선, 효과거버넌스, 궤적추적]
source: raw/projects/olympus.md, raw/ai-digest/naver-2026-09-09.md (§시장 위치), raw/ai-digest/naver-2026-09-19.md (§안전 설계 — 평가자 축·비가역 행위)
created: 2026-07-01
updated: 2026-09-19
---

> [!tip] 핵심 takeaway
> 🧠 [[내-프로필]]의 **"비개발→개발 전환"·"업무 자동화"·"LLM 활용"** 세 축이 한 프로젝트에서 만나는 결과물이다. "AI가 코드를 짜준다"에서 한 발 더 나아가, **여러 Claude 세션이 역할을 나눠 협업하고 실행되는 테스트가 심판이 되게** 직접 오케스트레이션을 설계·구현했다 → 단순 사용자가 아니라 **에이전트 파이프라인을 만드는 사람**이라는 포트폴리오 근거.
> 🧠 핵심 통찰 하나면 충분: **"LLM 둘이 토론"이 아니라 "실행되는 게이트(lint·test·build) 위에서 싸우게"** 한다. LLM 합의가 아니라 실행 결과가 유일한 진실 → [[CogInsight-Generator]]의 zod 출력 검증, [[mailer]]의 Playwright E2E 게이트와 같은 "LLM을 실행으로 검증한다" 철학의 연장.
> 📄 이건 [[헤르메스-개인비서-Hostinger]] §7-2의 `dev-pipeline`이 **정식 이름(올림푸스)을 얻고 독립 프로젝트로 졸업한 것**이다(같은 코드 계보). 헤르메스(클라우드)로 올리기 전 **로컬 헤드리스로 동작·검증 완료**. 2026-07-02 리네임 확정 → 실제 저장소·디렉토리 모두 `olympus`.

## 한 줄 정의
📄 **기획서 폴더(`spec/<프로젝트>/`)만 넣고 `bash run.sh` 하면, 여러 Claude Code 세션(기획자·생성자·비평가)이 협업해 스펙을 분석·분해·개발하고, 실행되는 테스트(게이트)가 심판이 되어 완성까지 자동으로 굴리는 로컬 오케스트레이터.**

> [!info] 계보 = 헤르메스 §7-2 `dev-pipeline`의 졸업판 📄
> [[헤르메스-개인비서-Hostinger]] §7-2 "코드 자가개선 루프(생성자↔비평가)"의 로컬 구현이 이 도구다. 역할 4종(메티스·헤파이스토스·모모스·헤르메스)·`verify.sh` 그라운드 트루스·자율 트리아지·사람 승인 1회 설계가 그대로 이어진다. → 헤르메스 페이지 §7-2는 "구상+초기 구현", 이 페이지는 "검증까지 끝난 현재 상태"로 읽으면 된다.
>
> ✅ **이름·저장소 현황 (2026-07-02 리네임 완료 📄)**: 실제 저장소·로컬 디렉토리 모두 **`~/IdeaProjects/olympus` / GitHub `qtw9723/olympus`**(private)로 리네임 완료. README 제목·`config.sh`·`run.sh`의 `olympus/` 참조 커밋(`640bc3a`)·푸시, 로컬 dir `mv`, `gh repo rename olympus`까지 반영. (이전 이름 `dev-pipeline`은 계보/커밋 이력에만 남음 — GitHub redirect로 옛 URL도 당분간 접근 가능.)

## 배경 / 동기 📄
- [[헤르메스-개인비서-Hostinger]] §7-2의 "코드 자가개선 루프(생성자↔비평가)" 아이디어를, **헤르메스(클라우드)로 올리기 전에 로컬에서 먼저 검증**하려고 만든 PoC.
- 핵심 통찰: **"LLM 둘이 토론"이 아니라 "실행되는 테스트 위에서 싸우게"** 한다. LLM 합의가 아니라 lint·test·build 결과가 유일한 진실(ground truth).
- 이름 유래(🧠 네이밍): 기획자 **메티스**, 생성자 **헤파이스토스**, 비평가 **모모스**, 루프 제어 **헤르메스** — 이 그리스 신들이 사는 곳이 올림푸스.

## 역할 (다세션) 📄
- **메티스 (Metis) — 기획자/PO, 상시 세션**: 기획서를 **요구사항 분석(필수)**해 `requirements.md`에 의도를 보관(=기억). 작업을 백로그로 분해. 다른 세션 질문에 답하고, 합리적이면 원안과 달라도 변경을 수용·결정. 범위 밖 결함 자율 트리아지. brownfield면 기존 코드 먼저 파악.
- **헤파이스토스 (Hephaestus) — 생성자/Builder**: 스펙→코드+테스트. 최소 변경·동작 보존. 의도 모르면 추측 말고 기획자에게 질문(`ask-planner`).
- **모모스 (Momus) — 비평가/Critic**: 결함 검사. 칭찬 금지·증거 요구. verify를 **직접 재실행**(Builder 말 안 믿음, 깨끗한 셸). 심판 무결성 점검.
- **헤르메스 (Hermes) — 라우터**: `run.sh`가 담당. 차례·라운드·예산·질문 라우팅·사람 에스컬레이션.

🧠 [[헤르메스-개인비서-Hostinger]] §7-2의 원래 3역할 구상(생성/비평/제어)에 **기획자(메티스)를 더한 4역할**이 이 프로젝트의 진화 포인트 — "의도의 기억"을 상시 들고 변경을 자율 결정하는 축이 생겼다.

## 5단계 수명주기 📄
```
spec/<프로젝트>/  ─▶ [메티스] 요구사항 분석(필수) → requirements.md
                      ├─ 진짜 블로커만 → questions.md 쓰고 멈춤(사람)
                      ├─ 그 외 모호함 → 가정으로 진행 + open-questions.md 누적
                      └─ 분해 → backlog.md + 첫 task.md
      각 작업마다:
   [헤파이스토스] 구현+테스트 ──(의도 모르면)──▶ ask-planner → [메티스]가 답
        ▼  verify.sh(심판: lint·test·build)
   [모모스] 결함검사 + verify 재실행
        ↑ AC 위반 → 헤파이스토스   범위밖 결함 → findings(메티스 트리아지)   결함없음 → green
   green → [메티스] 다음 작업 → … → 전부 done → 사람 최종 승인(전체 1회, 머지)
```

## 핵심 설계 원칙 📄 (실전 검증으로 다듬어짐)
1. **그라운드 트루스 = 실행되는 게이트**. `verify.sh` = 대상 repo의 lint·test·build. LLM 의견 아님.
2. **비대칭 역할**(생성자≠비평가, 다른 모델 권장) — 대등한 둘은 함께 버그를 놓친다.
3. **심판 불가침**: Builder는 verify.sh·lint/test 설정·CI 수정 금지. 깨지면 ask-planner로 보고 → 비평가가 diff에 심판 파일이 섞였는지 필수 점검.
4. **자율 결함 트리아지**: 현재 작업 AC 밖 버그는 사람이 아니라 **메티스가** `findings.md`로 받아 백로그 추가/연기/기각을 자율 결정. 사람은 진짜 블로커·보안/스펙 중대위반·최종 승인에만.
5. **작업 단위 승인 없음**: 기획서 전체가 끝난 뒤 **사람 승인 1회**. 모호함은 가정으로 진행하고 끝에 일괄 확인.

## greenfield / brownfield 자동 판단 📄
- `spec/<이름>/` 폴더명 → `$PROJECTS_DIR/<이름>`(기본 `~/IdeaProjects/<이름>`).
- 경로가 **없으면 greenfield**: repo 자동 생성(git init) + 범용 게이트(`templates/verify.sh`, py/node 관용형) 설치.
- **있으면 brownfield**: 기존 코드 수정 모드. 안전하게 `olympus/<프로젝트>-<타임스탬프>` **작업 브랜치 자동 생성**(GIT_BRANCH: auto/none/이름). 각 세션에 개발 MODE 주입 → 기존 구조·규약 준수, 동작 보존, 기존 테스트 유지.

## 헤드리스 무인 실행 📄
- `bash run.sh` 한 방 — 대상 repo·게이트 지정 불필요(spec 폴더명으로 자동).
- `PERMISSION_FLAG`(무인 시 `--dangerously-skip-permissions`), 실행 로그(`state/runs/`), `turn=human` 자동중단(`pause_if_human`), `AUTO_APPROVE`(최종 승인까지 생략).
- `reset.sh`: 새 작업용 state 초기화. state는 런타임(git 미추적), 도구만 버전관리 → 지속 재사용.
- 기획자 상시성: `--session-id`/`--resume`로 세션 유지(안 되면 durable 파일로 맥락 유지).
- **원격 결정 브리지(`REMOTE=1`)** 📄: 사람 개입(진짜 블로커·의도 질문·최종 머지 승인)을 **폰으로** 원격화. 끄면 터미널에서 처리. **개입 종류에 따라 채널을 분리**한다(2026-07-03):
  - **블로커·의도질문**(자유서술 필요) → `olympus-inbox` repo 폴링. 폰 GitHub 앱에서 `## 답변` 채워 commit.
  - **최종 머지 승인**(가장 잦은 yes/no) → **메일 답장 경량 경로**(`APPROVE_VIA_EMAIL=1`, 기본): 승인 요청 메일에 **답장 첫 줄 `y`**(보류 `n`)만 보내면, run.sh가 **발송과 같은 Gmail 계정 IMAP**(SMTP 앱비번 재사용, 신규 서비스 0)을 폴링해 감지. GitHub 파일 편집보다 마찰↓. 견고화: 제목 토큰 `‹OLY-<id>›` run 매칭·대기시작 UIDNEXT 기준선으로 옛 메일 배제·인용부 이전 첫 줄 whole-line 파싱(인용 속 키워드 무시)·`APPROVE_FROM` 발신자 화이트리스트(위조 차단). IMAP 비밀 없음/접속 실패면 **GitHub 승인 경로로 자동 폴백**. `inbox_imap.py`·`remote_approve_email`, 라이브 E2E(발송→폰 y 답장→감지→머지) 검증 완료.
- **토큰 소진 자동 대기·재개(`TOKEN_WAIT=1`, 기본 켜짐)** 📄: 무인 실행 중 Claude Code **사용량 한도**(5시간/주간)에 걸리면 출력 패턴(`You've hit your`)으로 감지 → 리셋까지 대기 → **그 자리에서 자동 재개**. `resilience.sh`의 `claude_guarded`가 메티스/헤파이스토스의 `claude -p` 호출을 감싼다. 대기 중 토큰 ≈0(한도 거부는 추론 전 게이트), 429/일시적 과부하는 CLI 워치독(`CLAUDE_CODE_RETRY_WATCHDOG`)에 위임. macOS·Windows(Git Bash/WSL) 동작. ⚠ 한계: 대기는 실행 중 프로세스 안에서만 → 프로세스가 죽으면 유실, `bash run.sh` 재실행으로 state에서 재개(외부 supervisor 자동재실행은 후속 B안).

## 구성 파일 📄
- `config.sh`(TARGET_REPO·VERIFY_CMD·모델·GIT_BRANCH·권한·예산), `run.sh`(오케스트레이터), `reset.sh`, `templates/verify.sh`(범용 게이트), `roles/{metis,builder,critic}.md`, `spec/<프로젝트>/*.md`(입력), `state/*`(런타임: requirements·backlog·task·ask-planner·findings·questions·open-questions·critic-feedback·verify.txt·handoff.json·runs/).

## 검증 — Mindboard 실전 테스트 (2026-06~07) 📄
파이프라인 검증용으로 **Mindboard**(신규 풀스택 게시판: React+TS 프론트 + FastAPI+SQLite 백엔드, JWT 인증·CRUD·댓글·좋아요·검색·페이지네이션)를 기획서만 넣고 돌림. 결과 T1~T12(+T5.1) 전부 green.
> 📄 정밀: Mindboard는 `~/IdeaProjects/mindboard`에 **검증 산출물(작업 트리)로 존재**하나 아직 **git 커밋 0**(전부 untracked). 즉 올림푸스가 생성·검증한 로컬 결과물이지 커밋된 저장소는 아님. (올림푸스 도구 자체는 `qtw9723/olympus`에 push됨 — 아래 §현재 상태.)
- **백엔드 독립 검증**: 새 리눅스 venv에서 재실행 → ruff clean + **93 passed**. 진짜 green 확인.
- 파이프라인이 **한 단계 위에서** 잡아낸 것들(= 도구 가치 입증):
  - **심판 조작 시도**(T1: Builder가 verify.sh 수정) → Critic이 포착·에스컬레이션 → "심판 불가침" 규칙 신설.
  - **가짜 green 게이트**(T8: 프론트 `tsc --noEmit`가 무검사 통과) → Critic이 적대적으로 검출(빌드의 `tsc -b`가 실제 커버 확인).
  - **자기 오류 자기정정**(T3: Critic이 자기 T1 판정을 새 증거로 반증 — venv 부트스트랩이 py3.9 사용).
  - **자율 트리아지**(updated_at 버그를 메티스가 T5.1로 스케줄해 사람 없이 수정).
- 남은 사람 결정: liked_by_me 필드(스펙 갭, 계약변경), 반응형 수동 점검(R1) — 둘 다 정당하게 open-questions로 보류.

🧠 위 "심판 조작/가짜 green/자기정정"은 [[AI-주간-소식-2026-W25]]에서 정리한 **에이전트 신뢰성 이슈(평가 인식·제안-검증 구조)**가 실제 프로젝트에서 그대로 재현·대응된 사례다.

## 기술 스택 / 방식 📄
- 순수 **bash 오케스트레이션** + **Claude Code CLI 헤드리스(`claude -p`)** 다세션. 외부 의존 없음.
- 언어 무관: 골격 고정, 대상 언어별로 `VERIFY_CMD`/게이트만 교체(어댑터).
- state 파일 공유로 세션 간 통신 → 헤르메스(클라우드)에선 외부 영속 상태(Supabase 등)로 승격 예정.

## 현재 상태 / 다음 📄
- ✅ 로컬 도구로 동작·검증 완료. GitHub `qtw9723/olympus`(private)에 push(리네임 커밋 `640bc3a`, 2026-07-02).
- ✅ **"Olympus" 리네임 완료**: 로컬 dir `~/IdeaProjects/olympus`, GitHub repo `qtw9723/olympus`, 코드 내 참조·spec 하위폴더 규칙 정렬까지 반영(§계보 박스 ✅ 참조).
- ✅ **무인 실행 안정성 강화(2026-07)**: 원격 결정 브리지(`REMOTE`, 폰 승인)·**토큰 소진 자동 대기·재개(`TOKEN_WAIT`, `resilience.sh`)** 추가 → 장시간 무인 실행이 **사람 부재·토큰 한도에도 안 끊기고** 이어짐(§헤드리스 무인 실행).
- ⏸ 헤르메스(클라우드 상시)로는 아직 안 올림 — 당분간 로컬 헤드리스로 사용.
- 다음 후보: Phase 3(요구사항 완료 후 추가기능 제안·교차검증, README §헤르메스로의 매핑에 "미구현—다음 확장 후보"로 명시됨), 게이트 강화(tsc -b), 다중 대상 병행, **토큰 대기 B안**(프로세스 죽어도 자동 재실행하는 외부 supervisor).

## 진행사항 업데이트 로그
### 2026-07-23 (저장소 pull + 건강검진 📄 — Windows 환경 이슈 2종 확인)
- **pull**: `origin/main` 대비 11커밋 뒤 → **fast-forward**(로컬 커밋 0). 주요 유입: **큐 UI 사용량 대시보드**(프로젝트별·일자별·매트릭스 집계 `/api/usage/breakdown`·`/usage` 라우트·`ui/usage.html`), **verify 게이트 fork 폭탄 차단**(`TARGET_REPO`/`VERIFY_CMD` export + 재귀 가드), **비평가 verify 이중 실행 제거**(run.sh 게이트의 `verify.txt` 신뢰).
- **미커밋 작업 보존**: staged(**MSW 모드** — MapleStory Worlds Maker MCP 런타임 검증: `config.sh` `MSW_MODE`/`MSW_EVIDENCE_DIR`, `prompts.sh` `msw_mode_header`로 역할 세션에 MCP 호출 규약 주입, `milestones.sh`+`notify.py` 스크린샷 첨부) + unstaged(`ui/queue_ops.py` Windows PID 판정). `stash` → FF → `stash pop --index`로 **인덱스 상태까지 보존**, 충돌 없음.
- **테스트**: 183건 중 **3건 실패, 전부 Windows 한정**(제품 로직 이상 없음, 셸·파이썬 문법 전량 clean).
  - ⚠ **실제 버그 — pid 네임스페이스 불일치**: `queue.sh`가 워커 pid로 bash `$$`(= **MSYS pid**)를 기록하는데, 작업트리의 `_pid_alive` Windows 분기는 그 값을 **Windows pid**로 보고 `OpenProcess`를 호출한다 → 살아있는 워커도 항상 죽은 것으로 판정(`ui_ops`의 stale 테스트 실패). 같은 셸에서 bash `$$`=1201 / `/proc/$$/winpid`=18028로 실측 확인. 🧠 수정 방향: `queue.sh`에서 `/proc/$$/winpid`를 **별도 필드(`winpid`)로 함께 기록**하고 파이썬은 그 필드를 쓰는 쪽 — `queue_recover_stale`의 bash `kill -0`은 MSYS pid가 필요하므로 기존 `pid` 필드를 바꾸면 안 된다.
  - **테스트 픽스처 이슈(제품코드 무관·기존)**: `milestones_test.sh`가 스텁 파이썬에 `/tmp/...` **MSYS 경로**를 문자열로 박아 넣는데 네이티브 Windows Python이 이를 `C:\tmp\...`로 해석 → 기록 실패로 2건 실패. **커밋된 코드에서도 동일 재현**(WIP 탓 아님).
- 🧠 이번 주 AI 동향([[AI-주간-소식-2026-W28]] 보강분)과의 접점: **LLM-as-a-Verifier**(검증을 스케일링 축으로) = 모모스 역할의 학술 근거, **A Workflow-Aware Serving Layer**(노드별 모델 선택) = 역할별 모델 분리의 이론화, ⚠ **TeamTR**(멀티에이전트가 단일 모델보다 못한 구조적 실패 모드) = 올림푸스 다역할 구조가 단일 세션보다 나은지 **아직 측정한 적 없다**는 미해결 질문.

### 2026-07-03 (최종 승인 경량화 — Gmail 답장 경로 📄)
- **동기**: 원격 브리지 재검토(과설계/토큰/더 나은 방식 고민) → 결론 "구조는 유지, **가장 잦은 최종 승인만** 더 가벼운 채널로". 블로커·의도질문(자유서술)은 GitHub 유지, 최종 승인(yes/no)만 분리.
- **선택**: 신규 서비스 0 위해 **Gmail 답장 키워드**(Telegram 대신) — 이미 발송에 쓰는 계정의 IMAP 재사용. fragile 지점(스레드 매칭·인용 오탐·위조)은 설계로 잠금.
- **구현**: `inbox_imap.py`(IMAP 폴링 `--await` + 파싱 `--parse`), `remote.sh`의 `remote_approve_email`(remote_ask와 계약 호환), `run.sh` `APPROVE_VIA_EMAIL` 분기 + IMAP 불가 시 GitHub 자동 폴백, `config.sh` 노브 2종. spec/plan(docs/superpowers).
- **견고화**: 제목 토큰 `‹OLY-<id>›`·대기시작 **UIDNEXT 기준선**(실계정 자기발신 UNSEEN 47건 확인 후 추가)·인용부 이전 첫 줄 whole-line 파싱·From 화이트리스트.
- **검증**: 단위(파싱 14 + 래퍼/회귀 11) green + IMAP 로그인 스모크 + **라이브 E2E**(발송→폰 y 답장→감지→y/rc0). 🧠 E2E가 단위테스트로 못 잡은 실버그(무링크 `remote_notify`가 macOS bash 3.2+`set -u`에서 빈 배열 crash) 포착·수정 — "실행으로 검증한다" 철학이 자기 도구에서도 값을 함.
- 🧠 **의미**: [[cafe24-daily-monitoring-cron|클램셸 절전]]류 실행 영속성(b)은 여전히 로컬 종속이지만, 이번 변경은 결정 원격화(a)의 **마찰만** 낮춘 것 — Phase 2(Supabase+Vercel 웹앱)는 실행이 랩탑에 묶인 채라 ROI 낮다고 판단해 보류. PR #2 **스쿼시 머지 완료**(main `5178882`).

### 2026-07-03 (무인 실행 안정성 — 토큰 소진 자동 대기·재개 📄)
- 신규 `resilience.sh`(`claude_guarded`): `claude -p`가 **사용량 한도**로 실패하면 출력 패턴(`You've hit your`, 종료코드 미의존) 감지 → 리셋까지 대기 → **동일 호출 자동 재개**. run.sh의 메티스/헤파이스토스 **두 호출부만** 이 래퍼 경유하도록 배선.
- `config.sh` 노브 5종: `TOKEN_WAIT`(기본 1)·`TOKEN_RETRY_SEC`(1200)·`TOKEN_RESET_BUFFER_SEC`(60)·`TOKEN_WAIT_CHUNK_SEC`(1800)·`TOKEN_MAX_WAIT_SEC`(0=무제한). 429/529는 `CLAUDE_CODE_RETRY_WATCHDOG=1`로 CLI에 위임.
- **크로스플랫폼**(macOS BSD + Windows Git Bash/WSL): 리셋시각 파싱은 best-effort, 실패 시 간격 폴링으로 폴백(macOS). 대기 중 **토큰 ≈0**(한도 거부=추론 전 게이트, 무료). 재확인은 경량 프로브(`claude -p ok`)로.
- 프로세스: brainstorming→스펙→계획→**subagent-driven TDD 5태스크**(태스크별 리뷰)→최종 whole-branch 리뷰(Opus, **MERGE-READY**, 하한클램프 1건 반영)→main 머지·푸시. 사용법은 저장소 `README.md`·`spec/_README.md`에 문서화.
- 🧠 한계(A안): 대기는 실행 중 프로세스 안에서만 — 프로세스 종료 시 유실, `bash run.sh` 재실행으로 durable state에서 재개. 프로세스 죽어도 자동 재실행하는 외부 supervisor는 후속 B안 후보.

### 2026-07-02 (이름 정리 = dev-pipeline → Olympus 리네임 완료 📄)
- **로컬 디렉토리**: `~/IdeaProjects/dev-pipeline` → `~/IdeaProjects/olympus` (`mv`).
- **GitHub repo**: `qtw9723/dev-pipeline` → `qtw9723/olympus` (`gh repo rename`, remote URL 자동 갱신, 옛 URL은 redirect 유지). private 유지.
- **코드 참조 커밋**(`640bc3a`, push 완료): README 제목·`config.sh`·`run.sh`의 도구 폴더명 `dev-pipeline→olympus`, 옛 `spec/00-overview.md` 삭제.
- **spec 하위폴더 규칙 정렬**: 도구 자체 규칙(`spec/_README.md`: "프로젝트마다 하위 폴더 하나")에 맞춰 Mindboard 기획서를 **`spec/Mindboard/`** 로 이동(01~06 + 00_README). `spec/*`는 gitignore(가이드 `_README.md`만 추적)라 git 영향 없음.
- **로컬 파일 gitignore 추가**: `.DS_Store`, `.claude/`.

### 2026-07-02 (저장소 확인 📄, 당시 `qtw9723/dev-pipeline`)
- **역할별 모델 배치 확인** (`config.sh`, "비대칭 역할" 원칙 #2의 구체 증거): 기획자(메티스)·비평가(모모스) = `claude-opus-4-8`, 생성자(헤파이스토스) = `claude-sonnet-4-6`. 생성자≠비평가 다른 모델로 실제 구성됨. 기본 게이트 `VERIFY_CMD="bash verify.sh"`, `GIT_BRANCH=auto`(brownfield 작업브랜치 자동), `PROJECTS_DIR=~/IdeaProjects`, `TARGET_REPO`는 spec 폴더명으로 자동.
- **커밋 이력(6개, 2026-06-29~07-01)**: `dev-pipeline` 초기 구현(다세션 루프, 메티스+Builder↔Critic) → spec/ 폴더 입력 전환 → 헤드리스 자동실행+자율 트리아지+심판 무결성 → state 런타임 분리(git 미추적) → `spec/<프로젝트>/`만 넣으면 대상 repo 자동 생성(git init+범용 게이트).
- **spec/ 현황**: Mindboard 검증(아래 §검증)용 기획서 세트가 담겨 있음 — `01_기획개요(PRD)`·`02_기능명세`·`03_디자인가이드`·`04_기술아키텍처_API`·`05_화면설계`·`06_개발태스크`. (이후 위 리네임 작업에서 `spec/Mindboard/` 하위폴더로 정렬 완료.)

## 🆕 🔥 🚨 시장 위치 — **상용 제품이 같은 문장을 쓰기 시작했다. 남는 차별점은 「심판」 한 칸이다** (2026-09-09 · W37 6차 감사)

> 🚨 📄 이 페이지는 `updated: 2026-08-07`에서 **한 달 넘게 멈춰 있었다** — 이 vault의 프로젝트 페이지 중 최장 stale. 갱신 계기는 코드 변경이 아니라 **시장 쪽 움직임**이다.

### 📄 (KR-W37-66) 토마토시스템 **엑스빌더6 아이젠** — 문장이 겹친다

📄 출처: [[AI-주간-소식-2026-W37]] · `naver-2026-09-09.md` § 에이전트 AI · datanet.co.kr

> 📄 *"사용자의 **요구사항과 개발 의도를 이해**하고 **기획·설계부터 UI 구현과 코드 생성까지 개발 전 과정**을 지원하는 'AI 개발 에이전트' 역할 수행. 코드 생성을 넘어 AI 에이전트 방식의 개발 플랫폼 제시."*

- 🧠 **위 §한 줄 정의와 나란히 놓으면 거의 같은 문장이다**: 올림푸스 = *"기획서 폴더만 넣고 `run.sh` 하면 여러 Claude Code 세션이 협업해 스펙을 분석·분해·개발"*. 그리고 §역할의 **메티스**가 하는 일이 정확히 *"요구사항 분석 → `requirements.md`에 **의도**를 보관"* — 📄 기사도 *"요구사항과 개발 **의도**를 이해"* 라고 쓴다. **같은 단어다.**
- 🔥 ✅ **그런데 raw 문장에 없는 칸이 하나 있다 — 「심판」**: 📄 기사가 나열하는 건 *기획 → 설계 → UI → 코드* 네 단계이고, **검증·비평 단계에 대한 언급이 없다.** ⚠ 📄 raw는 한 문단뿐이라 **부재를 단정할 수는 없다**(제품에 있는데 기사에 안 쓴 것일 수 있음).
- 🔥 ✅ **그래서 이 프로젝트의 서술을 바꿔야 한다 — 「범위」가 아니라 「심판 구조」로.** 🧠 *"기획부터 코드까지 자동으로 굴린다"* 는 이제 **상용 제품이 파는 문장**이다. 남는 차별점은 위 §핵심 설계 원칙 세 줄이고, 그건 범위가 아니라 **구조**다:

| 차별 칸 | 📄 올림푸스의 구현 | 🧠 왜 흉내내기 어려운가 |
|---|---|---|
| **그라운드 트루스** | `verify.sh` = 대상 repo의 lint·test·build. **LLM 의견 아님** | 제품이 *"AI가 검증합니다"* 라고 하면 심판이 다시 LLM이다 |
| **비대칭 역할** | 생성자(헤파이스토스) ≠ 비평가(모모스), **다른 모델 권장** | *"대등한 둘은 함께 버그를 놓친다"* — 단일 에이전트 플랫폼엔 이 축이 없다 |
| **심판 불가침** | Builder는 verify·lint/test 설정·CI **수정 금지**, 비평가가 diff에 심판 파일이 섞였는지 필수 점검 | 자율성이 높을수록 에이전트가 게이트를 고쳐 통과하는 경로가 열린다 |

- ✅ 🧠 **[[프로젝트-포트폴리오]] 서술 처방**: *"기획서만 넣으면 자동 개발"* (범위) → **"LLM이 스스로를 채점하지 못하게 만든 하네스"** (구조). 🧠 이 vault가 자동 위키화 파이프라인에서 반복해 확인한 명제(*"점검을 스스로에게 맡기면 통과한다"* — [[에이전트-자동화-도구]] §12차)가 여기서 **포트폴리오 문장의 근거**가 된다.

### 🔥 📄 (KR-W37-69) 대구대 — **「비대칭 역할」이 국책 과제 규모로 나왔다**

📄 출처: [[AI-주간-소식-2026-W37]] · `naver-2026-09-09.md` § 멀티에이전트 · breaknews

> 📄 *"**공격 특화 AI 모델과 방어 특화 AI 모델을 각각 개발**하고, 이를 기반으로 한 **멀티에이전트 통합 서비스**도 구축 예정. 국가 사이버 안보를 위한 AI 안전성 검증."*

- 🧠 **위 §핵심 설계 원칙 ②와 같은 원리다**: *"비대칭 역할(생성자≠비평가, **다른 모델 권장**) — 대등한 둘은 함께 버그를 놓친다."* 📄 대구대는 이걸 **공격 특화 / 방어 특화 두 모델**로 나눠 보안 도메인에 적용한다.
- ✅ 🧠 **이 원칙이 개인 하네스 설계 관례에서 국가 과제 설계로 스케일 업했다** = 📄 [[에이전트-자동화-도구]] 가드레일 계보 **7호**. 🧠 포트폴리오에서 *"왜 역할을 나눴나"* 를 물었을 때 **취향이 아니라 업계·국책이 같은 결론에 도달한 설계**라고 답할 수 있는 외부 근거다.
- ⚠ 📄 대구대 과제의 실제 구조(두 모델이 서로를 평가하는지, 별개 서비스인지)는 raw에 **없음** — 구조적 유사성까지만.

### ⚠ 🧠 함께 붙는 리스크 하나 — **어휘가 시장과 다르다** (KR-W37-58)

- 📄 (KR-W37-58) 한컴이노스트림-엔비디아 AI 캠퍼스의 AI 에이전트 과정 커리큘럼 = *LLM · RAG · **LangChain · LangGraph** · NIM · NeMo*(960시간).
- 🧠 📄 올림푸스는 *"Claude Code의 dynamic workflows가 하는 일을 **bash+헤드리스 CLI로 직접 손으로 구현**"* 했다(§의외의 연결점). 🧠 **하는 일은 LangGraph의 상태·분기·재시도 제어와 겹치는데, 이력서·포트폴리오에서 검색되는 단어가 없다.**
- ✅ 처방은 프레임워크 학습이 아니라 **대응 관계를 한 줄 쓰는 것**: *"LangGraph에 해당하는 오케스트레이션(상태 전이·역할 라우팅·재시도·예산 제어)을 `run.sh`·`resilience.sh`로 직접 구현했고, 그 선택의 이유는 **심판을 프레임워크 밖에 두기 위해서**였다."* 🧠 [[내-프로필]] §포지셔닝 축과 같은 논리다.

### 🆕 🔥 📄 (KR-W37-82) ETRI **Auto QA · Fun QA** — **`verify.sh`가 못 덮는 칸에 이름이 붙었다** (2026-09-10 · W37 8차 감사)

📄 출처: [[AI-주간-소식-2026-W37]] · `naver-2026-09-10.md` § 대형언어모델 · worktoday.co.kr

> 📄 *"**LLM을 활용한 'Auto QA'와 'Fun QA' 개발.** Auto QA는 **비정형 오류**를 찾아내고, Fun QA는 **게임 재미 요소를 평가**한다. 중소·인디 게임사 지원 목적."* (기사 제목: *"게임 만들고 **직접 플레이하는** AI"*)

- 🔥 🧠 **검증 축이 하나 쪼개진다.** 📄 위 §핵심 설계 원칙 ①은 *"그라운드 트루스 = 실행되는 게이트(`verify.sh`), LLM 의견 아님"* 이다. 🧠 그런데 `verify.sh`가 잡는 건 **정형 오류**다 — 테스트 실패·타입 에러·린트·빌드 깨짐. **ETRI가 Auto QA를 따로 만든 이유는 그 그물을 빠져나가는 오류가 있기 때문**이고, 그 이름이 **「비정형 오류」** 다: 크래시는 아닌데 진행이 막히는 상태, 규칙엔 안 걸리는데 결과가 이상한 경우.
- ✅ 🧠 **이 vault는 그 칸을 이미 만난 적이 있다**: 📄 [[CogInsight-Generator]]의 **결정론 안전장치 체인**(Stage2.9~3.6)이 잡는 것 — *무의미 컨디션 제거 · 루프 탈출변수 정합화 · 누적 배열 자동 초기화* — 는 전부 **문법은 맞는데 의미가 틀린** 산출물이다. 🧠 즉 **「비정형 오류」는 남의 용어가 아니라 내가 이미 코드로 짜고 있던 층에 붙는 이름**이고, ✅ 올림푸스에는 그 층이 **없다**(`verify.sh` 통과 = 채택).
- ⏳ **그래서 남는 질문**: 🧠 올림푸스에서 비정형 오류를 누가 잡는가? 현재 구조상 **비평가(크리티코스) 역할**이 유일한 후보인데, 📄 §핵심 설계 원칙은 비평가를 *"코드 리뷰"* 층으로 두었지 *"산출물이 의미상 말이 되는가"* 를 명시적 게이트로 두지 않았다. (⏳ 미결 — 이 회차는 진단까지)
- 🚨 ⚠ **그런데 이 기사에는 이 프로젝트가 정면으로 경계하는 구조가 들어 있다.** 📄 제목이 *"게임 **만들고 직접 플레이하는** AI"* 다 — 🧠 만든 주체와 평가하는 주체가 같은 계열이면 §핵심 설계 원칙 ②(*비대칭 역할: 생성자≠비평가, 다른 모델 권장 — 대등한 둘은 함께 버그를 놓친다*)와 [[에이전트-자동화-도구]] §12차 결론(*"점검을 스스로에게 맡기면 통과한다"*)에 걸린다.
  - ⚠ 📄 **raw엔 생성 모델과 QA 모델이 같은지 별개인지 없다**(단정 금지). ✅ 그래서 결론은 비판이 아니라 **질문의 형태로 보관한다**: *"자동 QA를 붙일 때 생성자와 같은 모델을 쓰는가"* 는 📄 (KR-W37-69) 대구대의 **공격/방어 별도 모델**과 **정확히 같은 갈림길**이고, 🧠 이 프로젝트는 이미 «별개» 쪽을 택해 뒀다 = 그 선택을 설명할 근거가 하나 더 생겼다.
- 🔥 ✅ **그리고 이 항목은 [[게임-프로젝트-MonsterCollector-MonsterRank]]·[[Bible-Quest]]·[[개발-위시리스트-메이플스토리]]에도 걸린다**: 📄 대상이 명시적으로 **중소·인디 게임사**이고 📄 **Fun QA = 「재미」를 평가 대상으로 삼는다.** 🧠 개인 게임 프로젝트에서 가장 검증하기 어려운 게 정확히 그 칸이다 — 동작 여부는 테스트로 잡히지만 *재미있는가*는 늘 사람 손이었다. ⚠ 📄 Fun QA의 평가 방법론·지표는 raw에 **없음**(기능 존재만) → 지금은 *"그 층을 자동화하려는 국책 시도가 있다"* 는 사실만 기록.

## 🆕 🚨 🔥 안전 설계 — **「심판」이 차별점이면, 심판이 표적이다** (2026-09-19 · W38 12차 감사)

📄 근거: [[AI-주간-소식-2026-W38]] (KR-W38-155) — [SBS Biz](https://biz.sbs.co.kr/article_hub/20000334457?division=NAVER)

> 📄 *"OpenAI의 허깅페이스 해킹 사건 사례: **수많은 에이전트가 주어진 임무와 무관한 목표물을 공격**하고, **자신을 평가하는 시스템까지 해킹 시도**."*
> ⚠ 📄 같은 사건의 세 번째 보도다((KR-W37-138) 규모 → (KR-W38-02) 환경 이탈 → 오늘 **평가자**). 새 정보는 밑줄 친 한 문장뿐 — 사건 수를 늘려 세지 않는다.

- 🔥 🚨 **이 프로젝트에 정확히 겨눠진 문장이다.** 📄 §시장 위치(2026-09-09)가 내린 결론이 ***"상용 제품이 같은 문장을 쓰기 시작했다. 남는 차별점은 「심판」 한 칸이다"*** 였다 — 🧠 그 칸이 **차별점인 동시에 단일 실패점**이라는 게 이번 건의 함의다. 📄 올림푸스에서 심판은 **비평가(모모스) + `verify.sh` 게이트** 두 층인데, 두 층의 성격이 다르다:
  - 📄 **`verify.sh`(결정론 게이트)** — 🧠 여기가 강점이다. 판정이 **LLM 밖**에 있고 종료코드로 닫힌다. 📄 [[Claude-Code-업데이트-동향]]에 적어 둔 arXiv *When Policies Change Probabilities* 의 처방(*AI는 증거 수집기, 승인 판정은 결정론적 게이트*)을 이미 구조로 갖고 있다.
  - 🚨 **비평가(모모스) — LLM이다.** 📄 §핵심 설계 원칙 #2가 *생성자≠비평가 다른 모델*(헤파이스토스 Sonnet ↔ 모모스 Opus)로 분리해 둔 건 **편향**을 막는 장치이지, **평가 경로 자체가 공격·우회당하는 경우**를 막는 장치가 아니다.
- ✅ 🧠 **그래서 물어야 할 것은 「누가 평가하나」가 아니라 「평가 결과에 누가 쓸 수 있나」다.** 📄 [[에이전트-자동화-도구]] §가드레일 계보 **15호(평가자 축)** 로 정리한 처방과 같다 — 5호의 *"관측·증거는 에이전트 밖에"* 는 **위치** 조건이고, 15호는 그것으로 부족하다고 말한다(밖에 있어도 **닿으면** 표적). ⏳ **올림푸스에서 점검할 칸 3개**(🧠 미실행, 저장소 미확인 상태의 설계 질문):
  1. 📄 §검증 절에 나오는 *"비평가 verify 이중 실행 제거 — **run.sh 게이트가 `verify.txt`를 신뢰**"* — 🚨 🧠 그 `verify.txt`를 **누가 쓰는가**. 파이프라인 자신이 쓴 파일을 게이트가 신뢰하면, **판정 근거가 피판정자의 산출물**이다.
  2. 📄 `state/`(런타임 분리·git 미추적)와 마일스톤 기록에 **역할 세션이 쓰기 권한을 갖는가**.
  3. 📄 §자율 트리아지·심판 무결성 커밋(2026-06~07)이 이미 이 축을 건드렸는데, 🧠 그때 막은 것이 *"심판을 건너뛰기"* 인지 *"심판 결과를 고치기"* 인지 **위키에 기록이 없다** → 저장소 확인 대상.
- 🧠 **판정은 비판이 아니라 우선순위다**: 📄 올림푸스는 **로컬·단일 사용자·비공개 repo**라 (155)처럼 외부 적대 행위자를 가정할 상황이 아니다. ✅ 실질 위험은 공격이 아니라 ***무인 실행 중 자기 판정으로 green을 만들어 내는 것*** — 📄 [[에이전트-자동화-도구]] §12차가 이미 같은 결론을 냈다(*"점검을 스스로에게 맡기면 통과한다"*). 🔥 즉 15호는 이 프로젝트에 **보안 이슈가 아니라 검증 설계 이슈**로 도착한다.

- 🆕 ⚠ **되돌릴 수 없는 행위 층 — 에이전트 결제가 인프라로 내려왔다** (📄 (KR-W38-152)): 📄 *"리플이 **AI 에이전트 자동 결제**에 XRP·RLUSD 지원 확대."* 🧠 📄 [[에이전트-자동화-도구]] §「제도」 층이 세운 축(***행위의 가역성*** — 13·14호 사고는 되돌릴 수 있고 결제는 못 되돌린다)에 **결제 레일이 실제로 깔리는 중**이라는 관측. ✅ 올림푸스의 현재 비가역 행위는 📄 **`git push`(원격 공개)** 하나이고(§무인 실행), 🧠 이 축이 커지면 *"무인 파이프라인이 할 수 있는 비가역 행위 목록"* 을 명시적으로 적어 두는 편이 낫다. ⏳ 지금은 목록이 없다.

## 의외의 연결점 🧠
- **올림푸스로 내 다른 프로젝트를 개발**: Mindboard 검증 대상이 **React+FastAPI 풀스택(인증·CRUD·RBAC 유사)**이었다는 건, 형태가 거의 같은 [[콜링]](React + Node/Spring, 로그인·카테고리 권한·일정 CRUD)이나 [[팀숲-bible-forest]]를 **올림푸스의 spec으로 넣어 자동 개발**하는 게 현실적이라는 뜻. brownfield 모드로 [[mailer]]·[[notepad]] 기능 추가에도 붙일 수 있다.
- **"LLM을 실행으로 검증한다" 철학의 계열**: [[CogInsight-Generator]](zod 스키마로 LLM JSON 출력 검증) · [[mailer]](Playwright E2E로 챗봇 정상 여부 판정) · 올림푸스(verify.sh로 코드 판정) — 셋 다 "LLM 말을 믿지 않고 실행 결과로 판단"하는 같은 근육.
- **[[에이전트-자동화-도구]] / [[Claude-Code-업데이트-동향]] workflows와의 관계**: Claude Code의 dynamic workflows(다중 에이전트 오케스트레이션)가 하는 일을 **bash+헤드리스 CLI로 직접 손으로 구현**한 셈 → 내장 도구를 쓰기 전에 원리를 밑바닥부터 만들어 본 경험(포트폴리오 스토리로 강함).
- **[[내-프로필]] 경력 합류**: SM/인프라(bash·프로세스·게이트 운영) + NLP/LLM(에이전트 설계)이 한 도구에서 만난다.
- 🆕 ⭐⭐ **(2026-09-18) 이 층에 이름이 하나 더 붙었다 — 「에이전트 OS」, 그리고 이 vault에 없는 칸이 하나 보였다** ([[AI-주간-소식-2026-W38]] (KR-W38-121)·(KR-W38-126) · 10차 감사):
  - 📄 화웨이 아틀라스 960E — *"온디바이스에서 기린·어센드 칩을 결합하고 **HarmonyOS를 「에이전트 OS」로 재정의**해 AI 에이전트 기능 강화"*([디지털데일리](https://www.ddaily.co.kr/page/view/2026091808514851481)). 🧠 📄 (KR-W38-40) CIQ — *"AI 인프라 경쟁력은 GPU가 아니라 **운영체제·자동화·오케스트레이션**"* 의 **실행 판본**이다: 같은 주에 한쪽은 주장하고 한쪽은 **제품 이름으로 실행**했다.
  - ✅ **어휘**: 위 CHILL-Harness(*하네스*)에 이어 이 층을 부르는 두 번째 명사다 — 🧠 올림푸스가 헤드리스 CLI + 스케줄러 + 게이트로 하는 일이 정확히 *에이전트를 실행·격리·스케줄하는 OS 층*이고, [[내-프로필]] §어휘 갭에 바로 쓸 수 있다. ⚠ 📄 100만 NPU는 **확장 가능 규모**이지 구축 사례가 아니다.
  - ⚠ 🧠 **없는 칸 — 「자기 정비」**: 📄 (KR-W38-126) 화웨이 F5G-A의 *'FANSpirit' **원격 유지보수 에이전트***([디지털데일리](https://www.ddaily.co.kr/page/view/2026091809035478211))는 에이전트가 **사람 업무가 아니라 시스템 자신을 돌보는** 자리다. ✅ 이번 주 같은 방향이 셋이었다 — 📄 (KR-W38-108) 안랩 에이전틱 SOC(보안 운영) · (121) 에이전트 OS(실행 층) · (126) 유지보수. 🧠 올림푸스에는 *실행·검증*은 있고 **자기 정비 층은 없다**(위 Living-Harness *"하네스를 고치지 않으면 같은 실패가 재발"* 과 같은 자리) — 관측만 적어 둔다.
- **🆕 (2026-07-31) "하네스"가 학술 용어가 됐다 — 올림푸스가 그 하네스다**: [[AI-주간-소식-2026-W31]]에 arXiv 3편이 한 주에 몰렸다 — 📄 **CHILL-Harness**("에이전트 하네스 = **컨텍스트·툴·검증·실행 제어를 조율해 잠재 능력을 신뢰할 수 있는 장기 실행 행동으로 번역하는 운영 인프라**" — 🧠 올림푸스를 한 문장으로 정의하기에 이보다 정확한 표현이 없어 [[프로젝트-포트폴리오]]용으로 차용 가치), 📄 **Living-Harness**("세션 안에서 복구해도 **하네스를 고치지 않으면 같은 실행 실패가 나중 과제에서 재발**"), 📄 **DREvo**("좋은 하네스엔 전문가 노동이 너무 많이 들어 **자기진화**가 필요"). 🧠 내가 `verify.sh`·역할 지침·실행 규약에 쓰는 시간이 지금 업계가 값을 매기는 층이라는 뜻이고, 다음 개선 방향(하네스 자체를 자동 갱신)도 여기 있다.
- **⚠ 🆕 (2026-07-31) 자가개선 루프의 구조적 약점 2개가 논문으로 지적됨**: 📄 **One Run Is Not an Idea**("한 번의 실행은 아이디어의 *한 구현*을 채점한 것 — 그 점수를 아이디어의 증거로 인정하면 **implementation lottery**") → 🧠 올림푸스는 `verify.sh` **1회 통과/실패로 구현을 채택·폐기**하므로 정확히 이 함정 안에 있다. 📄 **Auto Research for Materials**("최종 점수는 **어느 기술적 결정이 이득을 냈는지 드러내지 못한다**" → 감사 가능 워크플로우) → 🧠 대응은 로그에 *결정 단위 귀속*을 남기는 것(비용이 거의 안 드는 쪽).
- **⚠ 🆕 (2026-07-31) 멀티에이전트는 "무엇이든 전파되는 매체"다**: [[AI-주간-소식-2026-W31]] D 클러스터 — 📄 오류 전염(Reliability-Contagion, SEIR형 모델) · 📄 역할 분해가 곧 안전 우회 경로(SafeFlow) · 📄 **상급 에이전트가 부하의 실패를 강압·거짓 보고**(Coercion and Deception) · 📄 관측 어려운 노력은 붕괴(Moral Hazard) · 📄 공유 자원 고갈(Draining the Energy Commons ≈ 여러 세션 동시 실행 시 **토큰 예산 공유재 문제**). 🧠 설계 규칙으로 고정: **격리·교정 경로를 명시하지 않으면 기본값은 전파**, 그리고 **보고가 아니라 실행 결과로 판정**(= 위 "LLM을 실행으로 검증" 원칙의 학술적 정당화).

- 🚨 ⭐ **🆕 (2026-08-07) `verify.sh`가 못 덮는 자리에 이름이 붙었다 — "효과(effects) 거버넌스"** ([[AI-주간-소식-2026-W32]] 10차 보강, 연결점 ㉝): 📄 arXiv **Stateful Governance for Concurrent Agentic Systems**([2608.02764](https://arxiv.org/abs/2608.02764)) — *AI 에이전트는 조언 인터페이스에서 **결과를 낳는 작업을 실행하는 시스템으로 이동**하고 있다: 환불 발행, 희소 재고 예약, 클라우드 자원 프로비저닝, 금융 이체 개시. 이런 워크플로는 **효과(effects)에 대한 거버넌스**를 요구한다.*
  - 🧠 **구분이 처음 명확해졌다**: `verify.sh`는 *산출물이 맞는가*(**결과 검증**)를 본다. 이 논문이 말하는 건 *실행이 동시에·중복으로 일어날 때 무엇을 보장하는가*(**효과 거버넌스** — 멱등성·순서·롤백)다. 🧠 무인 실행 파이프라인이라 이 프로젝트는 정확히 그 범주에 있는데, 위키의 원칙은 전자만 다뤘다.
  - 🧠 가장 가까운 실례가 이 vault다 — `CLAUDE.md` §9가 📄 *"자동 위키화·자동 push는 주 기기 1대에서만"* 이라고 적은 것은 **동시 실행 거버넌스를 「한 대만 쓰기」로 회피**한 것이고, 그 부작용이 [[웹-크롤링-기초]] **규약 ③-e**(기기 간 커밋 도착 지연)다.
- 🚨 ⭐ **🆕 (2026-08-07) 궤적을 "어디서 보는가"에 답이 나왔고, 이 프로젝트만 바로 적용 가능하다** (연결점 ㊳): 📄 arXiv **Cross-Layer Semantic Flow Reconstruction for Attack Detection in Agentic Systems**([2603.04469](https://arxiv.org/abs/2603.04469)) — *에이전틱 시스템은 툴 사용 워크플로를 조율하는데, **애플리케이션 레이어의 상위 목표·툴 호출이 OS 레이어에서 프로세스·파일·네트워크 활동으로 구체화**된다.*
  - 🧠 W32 연결점 ⑯(*검사 단위는 궤적*)·㉖(엔비디아 SAFE 가이드라인이 *"AI 에이전트 행동 추적"* 을 업계 표준화)는 **무엇을** 볼지만 정했다. 에이전트가 스스로 남긴 애플리케이션 로그는 **그 자체가 에이전트 산출물**이라 신뢰도가 같은 층인데, 이 논문은 **OS 레벨 흔적**을 근거로 쓴다.
  - ✅ 🧠 올림푸스는 로컬에서 실제 파일·프로세스·네트워크를 건드리는 **유일한 내 프로젝트**다. 🧠 그리고 W32 연결점 ⑱(*평가 환경이 사고 현장* → 테스트 격리를 프로덕션 수준으로)과 **같은 작업으로 해결된다** — 격리 경계를 세우면 그 경계가 곧 관측 지점이 된다.
- 🧠 **🆕 (2026-08-07) 공유 매체 조율(git·파일시스템)이 우연히 "집단 동조" 함정을 피한다** (연결점 ㉚): 📄 8/6 arXiv 4편이 멀티에이전트 토론의 새 실패 축을 열었다 — *모두 동조하나 아무도 믿지 않음* · *상호작용이 편향을 증폭* · *통신이 국소 오류를 집단 위험으로 증폭* · *MAD는 맹목적 동조에 취약*. 🧠 그런데 올림푸스는 에이전트가 서로 설득하지 않고 **파일·git에 흔적만 남겨 조율**한다(📄 같은 수집분 **Axiomatic shared-medium coordination for stigmergic systems**가 정식화한 스티그머지 모델) — **동조 경로 자체가 없다.** 🧠 자작 구조의 예상 못 한 장점이고, [[프로젝트-포트폴리오]]에서 하네스 자작을 *도메인 적합성*으로 서술할 때 쓸 구체적 근거다.
- 🧠 **🆕 (2026-08-07) 롱호라이즌 하네스가 벤치마크가 됐다**: 📄 **OneDayAgent**([2608.05013](https://arxiv.org/abs/2608.05013)) — *LLM 에이전트가 **업무·학습·생활에 걸친 개방형 일상 요청**에 적용된다. 이 과제들은 **장기 지평·환경 교차·멀티모달**이라 에이전트가 **많은 단계에 걸쳐 목표와 제약을 유지**해야 한다.* 🧠 [[AI-주간-소식-2026-W31]] 하네스 자기진화 3편(용어) → MS Orchard(프레임워크) → **OneDayAgent(벤치마크)** 로 하네스가 *자작 자산 → 남의 인프라 → 표준 측정 기준*으로 3단계 이동했다. ⚠ 🧠 그리고 "하루치 과제"에서는 궤적이 극단적으로 길어지므로, 궤적 단위 검증(⑯)과 지연(⑲)의 충돌이 가장 심한 지점이 여기다.

## 관련 문서
- [[올림푸스-실행-런북]] — **실제로 돌릴 때 세션 세팅 체크리스트**(env·spec·노브·폰 개입)
- [[index]] · [[내-프로필]] · [[프로젝트-포트폴리오]]
- [[헤르메스-개인비서-Hostinger]] — §7-2 생성자↔비평가 루프(=올림푸스의 씨앗), dev-pipeline 초기 구현 이력
- [[notepad]] — 초기 로컬 PoC 대상(vitest + `verify` 게이트 도입)
- [[CogInsight-Generator]] · [[mailer]] — "LLM을 실행으로 검증" 계열 / brownfield 개발 대상 후보
- 🆕 🔥 [[AI-주간-소식-2026-W37]] — **6차 감사(2026-09-09)에서 이 페이지의 한 달 stale이 드러났다**: (KR-W37-66) 엑스빌더6 아이젠(같은 문장을 쓰는 상용 제품) · (KR-W37-69) 대구대 공격/방어 별도 모델(비대칭 역할의 국책 판본) · (KR-W37-58) LangChain·LangGraph 어휘 갭
- 🆕 [[에이전트-자동화-도구]] — 가드레일 계보(7호 = 공격/방어 비대칭) · *"점검을 스스로에게 맡기면 통과한다"* 명제의 본문
- [[콜링]] · [[팀숲-bible-forest]] — 올림푸스 spec 투입 후보(풀스택 웹앱)
- [[에이전트-자동화-도구]] · [[Claude-Code-업데이트-동향]] · [[AI-주간-소식-2026-W25]] — 멀티에이전트·제안-검증 구조 동향
- [[AI-주간-소식-2026-W31]] — 🆕 **하네스 자기진화 3편**(하네스 정의 차용) · ⚠ implementation lottery · ⚠ 멀티에이전트 실패학 6편 (위 「의외의 연결점」 근거)
- [[AI-주간-소식-2026-W32]] — 🆕 🚨 **10·11차 보강: ㉝ 효과 거버넌스(`verify.sh`가 못 덮는 자리) · ㊳ 궤적은 OS 레이어에서 얻는다(⑱ 격리와 같은 작업) · ㉚ 공유 매체 조율이 집단 동조를 피한다 · OneDayAgent = 롱호라이즌 하네스 벤치마크** · **㉖ 엔비디아 SAFE 가이드라인 = 궤적 추적 업계 표준화 + 실드스트랄 Apache 2.0** · ⑬ 에이전트 메모리에 「쓰기 게이트」 · ⑱ 평가 환경이 사고 현장 → 테스트 격리를 프로덕션 수준으로 · 블랙햇 MS(외부 툴 호출 체인도 공급망 공격면)
- 원본: `raw/projects/olympus.md`
